import { Box, Typography } from "@mui/material";

const NoData = () => {
  return (
    <Typography py={2} textAlign={"center"} color={"error"} fontWeight={"600"}>
      No Data Found
    </Typography>
  );
};
export default NoData;
